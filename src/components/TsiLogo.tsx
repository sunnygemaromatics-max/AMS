/**
 * Logo placeholder. The deployed brand uses a text wordmark only; this
 * component exists as a no-op so any older imports keep compiling.
 * Replace with an <img> or SVG when the client provides a logo file.
 */
interface TsiLogoProps {
  size?: number;
  className?: string;
  animated?: boolean;
}

export function TsiLogo(_props: TsiLogoProps) {
  return null;
}
